---
name: 0-询问luohe
description: 技能路由指南
disable-model-invocation: true
---

# 0-询问luohe — 技能路由器

一条**流程**是一条穿过技能的路径。大部分工作沿着**主流程**走。

## 主流程：想法 → 交付

1. **`/1-规划`** — 方案追问 → 领域建模 → 接口设计 → PRD → 任务拆解
   - 有代码库时从这里开始（维护 glossary-only 的 CONTEXT.md；ADR 单独写入 `docs/adr/`）
   - 默认批量询问（frontier 中独立决策集中问）
   - 用户说"逐步"/"一步一步"时切换为一次一问

2. **分支 — 状态机、算法或 UI 假设不确定？**
   - **是** → `/1-规划` 建立 `docs/prototypes/<topic>/` 的 throwaway prototype 验证任务；结论回写 `docs/plans/<topic>/`，原型默认不进入生产
   - **否** → 继续任务拆解

3. **分支 — 多会话构建？**
   - **是** → `/1-规划` 产出任务清单 → 清空上下文 → 每个任务用 `/2-开发`
   - **否** → 同一上下文直接 `/2-开发`

4. **`/2-开发`** — TDD 红-绿-重构循环 → 自检（测试+类型+linter）→ 携带审查基点、需求来源和 diff 审查意图交给 `/3-检查` 正式验收（保持未提交）

5. **`/3-检查`** — 基于输入契约强制 Review，输出 PASS/PASS WITH WARNINGS/FAIL 正式交接产物

6. **`/5-版本管理`** — Review 通过后，仅在用户明确授权时 commit + push（可选）

### 并行加速（实验性）
- **`/multi-worker`** — 需要完整设计文档 + 独立子任务 + Agent配置验证

## 上游（汇入主流程）

- **陌生代码库 / 看不懂代码** → 先进行一次性只读调查，输出 `docs/analysis/<topic>.md`（项目地图、已验证事实、未知项与停止条件）；调查完成即停止。根据结果：需要决策或实施方案 → `/1-规划`，可按现有输入做验收或架构评估 → `/3-检查`，需要定位根因或修复 → `/4-调试`。不新增 skill。
- **难搞的 bug** → **`/4-调试`** 构建回路 → 复现 → 假设 → 工具验证 → 修复 + 回归测试
- **代码腐烂 / 模块臃肿** → `/3-检查`，明确输入意图为“架构评估”；先评估架构与测试 seam，输出 `docs/analysis/<topic>.md` 和可追踪的 `docs/plans/<topic>/` 改造任务。评估完成即停止，由用户决定是否进入 `/1-规划`。

## 支撑层（不进主线，按信号触发）

| 信号 | 技能 |
|------|------|
| 新项目开张 | **`/0-启动`** 最小脚手架：项目结构 → 本地 git → uv 环境（其他按需加） |
| 初始化 AI 协作规则 | **`/0--claude`** 初始化 CLAUDE.md（称呼规则 + Karpathy 准则 + 工作流路由） |
| 工具输出太大 / token 紧张 / 要求极简表达 | **`/0--tokenless`** 超压缩沟通模式（Caveman） |
| 收尾、沉淀本次会话产出、清临时文件 | **`/6-最后整理`** 修改总结 + 经验沉淀 + 项目结构整理 + 交接文档 |
| 全局文档↔代码洁癖同步、防记忆膨胀 | **`/0--neat-freak`** 尺寸体检 + 记忆毕业 + 三层知识同步 |
| 复杂问题拿不准方向 | **`/0--dialectic`** 辩证矛盾分析法（六步法：调查研究→定性→矛盾分析→阶段划分→策略制定→持续复盘） |

## 个人定制（特定场景）

- **`/0--laoyoutiao`** — Python 交付节奏管理（面向甲方交付：复用现有开关、逐步展示优化成果）

## 快速判断

| 你的情况 | 用什么 |
|---------|--------|
| 新功能，方案不明确 | `/1-规划` |
| 新功能，方案明确，代码简单 | 直接 `/2-开发` |
| 新功能，方案明确，代码复杂 | `/1-规划`（生成任务清单）→ `/2-开发` |
| 看不懂代码 / 接手陌生项目 | 一次性只读调查 → `docs/analysis/<topic>.md` 项目地图与停止条件 → 按结果进入 `/1-规划`、`/3-检查` 或 `/4-调试` |
| 方案拿不准（状态机、算法、UI） | `/1-规划` → `docs/prototypes/<topic>/` throwaway prototype → 结论回写 `docs/plans/<topic>/` |
| Bug 难复现、性能突然下降、要找根因或立即修复 | `/4-调试` |
| 独立 Review：已有 base/fixed point、spec/需求来源和 diff | `/3-检查`（强制 Review，不问模式） |
| 只建单：有问题现象且明确要记录/建 issue | `/3-检查`（Bug 报告，只建单） |
| 根因修复：要求排查原因或立即修复 | `/4-调试`（传递已有现象/issue） |
| 代码越来越乱、模块太臃肿 | `/3-检查`（明确“架构评估”意图）→ `docs/analysis/<topic>.md` seam 评估 + `docs/plans/<topic>/` 改造任务 → 用户决定是否 `/1-规划` |
| 会话要结束了 | `/6-最后整理` |
| 新项目开张 | `/0-启动` + `/0--claude` |
| 想要极简回复 | `/0--tokenless` |
| 文档和代码不一致了 | `/0--neat-freak` |
| 复杂战略问题 | `/0--dialectic` |
| 不知道用啥 | 你现在这里 😊 |

## 上下文管理原则

### 何时清空上下文
- **主流程 1→2→3 保持一个上下文** — grilling、spec、任务清单都在同一窗口，共享理解
- **每个 `/2-开发` 开始前清空** — 从任务清单的单个 issue 出发，不带 grilling 的详细讨论

### 何时用 `/6-最后整理`
- 完成一个阶段（如 `/1-规划` 完成、多个 `/2-开发` 全部完成）
- 结束一天工作
- 把项目交给别人

### 何时用 `/0--neat-freak`
- `/6-最后整理` 完成后，如果发现文档和代码不一致
- 定期维护（如每周一次）
- CLAUDE.md 或 memory 明显膨胀时
