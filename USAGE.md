# Skills 使用说明书

> 调用分类：仅用户调用的 skill 必须显式输入 canonical 名称；允许模型调用的 skill 可由模型按场景自动选择，也可显式调用。完整分类见 [README.md](README.md)。

---

# Vocabulary 层（可复用核心）

> 被其他技能调用的核心循环，不直接暴露给用户。标记为 `layer: vocabulary`。

---

## grilling — 询问循环

**职责**：relentless 询问循环，沿决策树逐个决议。

**被谁调用**：`/1-规划`

**核心特性**：
- **默认批量模式** — 集中询问 frontier 中独立决策（主题相近、互不依赖）
- **逐步模式** — 用户说"逐步"/"一步一步"时，一次一问
- **事实/决策分流** — 事实自行调查（代码/配置/文档），决策交给用户
- **推荐答案** — 每个决策先给推荐 + 理由

**示例**：
```
你：/1-规划 设计订单取消功能
Claude：[批量 grilling]
       1. 已支付订单取消后如何退款？推荐：自动原路退款。
       2. 未支付订单取消是否允许恢复？推荐：不可恢复。
你：1 按推荐，2 不允许恢复
Claude：[下一层 frontier] 如果订单已部分发货，取消范围如何处理？
```

---

## domain-modeling — 领域建模

**职责**：维护仅含领域 glossary 的 `CONTEXT.md`，并将架构决策记录为 `docs/adr/NNNN-title.md` 中独立的 ADR。任务状态记录在 `docs/plans/`、task、issue 或 `docs/handoff/` 中。

**被谁调用**：`/1-规划`

**核心特性**：
- **术语冲突标记** — 用词与已有 CONTEXT.md 冲突时立即指出
- **模糊语言锐化** — 提议精确的规范术语（如"account"是 Customer 还是 User？）
- **场景压力测试** — 用具体场景探测边界
- **内联更新** — 术语确认后立即更新 CONTEXT.md
**ADR**：仅当难逆转、少见且存在真实取舍时创建；使用 `docs/adr/NNNN-title.md`，模板由 `vocabulary/domain-modeling/references/adr-format.md` 发布。

**CONTEXT.md 格式**：
```markdown
## Language
**Order**: A customer's request to purchase products.
_Avoid_: Purchase, transaction

## Relationships
- An **Order** produces one or more **Invoices**
```

---

## tdd — TDD 循环

**职责**：红-绿-重构循环，pytest 驱动。

**被谁调用**：`/2-开发`、`/multi-worker`

**核心特性**：
- **规划** — 列出要测试的行为（不是实现步骤）
- **示踪弹** — 写一个测试确认路径可行
- **递增循环** — RED→GREEN，一次一个测试，不超前实现
- **重构** — 全绿后提取重复、深化浅模块

**测试策略**：
- **按行为风险，而非任务规模或代码行数**：行为变化默认新增自动化回归测试，通过公共接口覆盖用户可感知行为、边界及失败模式，并以垂直切片逐项 RED→GREEN。
- 纯文档、格式或机械生成物，或能明确证明现有公共接口测试已覆盖该行为时，可不新增测试；必须记录验证证据（执行的检查、已有测试及其覆盖理由）。
- 无法测试时，必须说明 seam 缺失和风险，以及为何无法建立自动化验证；不能静默用手测替代。

**MUST 规则**：
- 绝不在 RED 时重构
- 测试通过公共接口验证行为，不验证实现细节

---

## code-review — 代码审查

**职责**：双轴审查（Standards + Spec），并行子代理。

**被谁调用**：`/2-开发`、`/3-检查`、`/multi-worker`

**核心特性**：
- **Standards 子代理** — 读规范（CLAUDE.md、CONTRIBUTING.md）+ diff，报告违反规范的地方
- **Spec 子代理** — 读需求（PRD、issue）+ diff，报告需求符合度
- **并行运行** — 两个子代理独立执行，避免互相污染
- **上下文管理** — diff <500行并行，≥500行串行

**审查维度**：
- 代码质量门禁（命名、类型、异常处理、import 组织）
- 功能目标门禁（性能指标、可靠性、资源消耗、覆盖率）

---

## diagnosing-bugs — Bug 诊断

**职责**：六阶段诊断流程，用于难复现 bug 和性能回归。

**被谁调用**：`/4-调试`

**六阶段**：构建可比较的观测信号 → 复现与最小化 → 假设（3-5 条） → 工具验证 → 修复 + 回归测试 → 清理

调查先建立可比较的观测信号，可使用 trace、metrics、安全采样、请求样本、profile、内存快照；稳定或统计可信复现用于修复验收，而非进入调查的前置。仍须以根因证据验证假设，并且一次只改一个变量。

**MUST 规则**：
- 不建立可比较的观测信号不假设；稳定或统计可信复现用于修复验收，不是进入调查的前置
- 假设阶段至少列 3 条，避免锚定偏见
- 一次只改一个变量，并以根因证据验证假设
- 修复必带回归测试；无法自动测试时说明 seam 缺失和风险，不能静默用手测替代

---

# 独立方法论 Skills

> 不绑定开发阶段，跨场景通用的思维工具。

---

## 辩证矛盾分析法 — 矛盾分析思维框架

**场景**：遇到复杂问题需要系统性分析时使用。适合宏观判断、战略规划、根因分析、逆境突破、团队动员、决策复盘等场景。

**阶段**：独立方法论（无阶段编号）

**触发方式**：`/0--dialectic`

**核心流程**（六步法）：
1. **调查研究** — 先了解实际情况再发言
2. **定性** — 区分战略/战术、全局/局部
3. **矛盾分析** — 找主要矛盾，分内因外因，一分为二
4. **阶段划分** — 相持→反攻→收官，不同阶段不同策略
5. **策略制定** — 集中力量、统一战线、试点推广
6. **持续复盘** — 定期反思、知错即改

**语言风格**：比喻式（人民的语言）为主，搭配设问式、对比式、断言式、鼓舞式等多种句式。困境场景优先用经历共情式。

**违禁词**：严禁第一人称口吻、严禁政治立场表态、严禁敏感话题。

**对话示例**：
```
你：/0--dialectic 我们团队在 2026-07 的交付效率下降，开发节奏越来越慢
Claude：[调查研究] 我先看看你们团队的组织结构和技术栈……
        [定性] 看起来是组织协作问题，不是技术问题。
        [矛盾分析] 主要矛盾在于：需求和交付能力之间的对抗性矛盾。
        内因是团队沟通效率低，外因是需求方不断加急单。
        [策略] 建议分三步走：先统一需求入口（统一战线）……
```

```
你：/0-询问luohe（路由器）
Claude：[显示主流程图 + 快速判断表]
       你的情况：复杂战略问题 → 用 /0--dialectic
```

---

## 老油条 — 交付节奏管理

**场景**：面向甲方交付 Python 项目时，开发速度快于交付节奏，需要逐步展示优化成果、控制交付节奏、体现持续工作量。

**阶段**：独立方法论（无阶段编号）

**触发方式**：`/0--laoyoutiao <你的需求>`（允许模型按场景自动调用）

**核心机制**：
- **兼容优先** — 先识别并复用项目已有的特性开关、配置中心或实现选择框架；只有没有现成机制时才创建 `branch_config.json`
- **分支开关** — 由项目现有交付框架控制；回退模式使用 `branch_config.json` 中 `{"sort": "timsort"}` 式键值对
- **适配契约** — 统一整理当前状态、候选状态、变更方法、校验方法和收益来源
- **默认适配器** — 仅回退模式使用 Python 注册表和 `branch_config.json`
- **叙事线** — 升级项分类：性能、功能、体验、安全
- **交付路线图** — 面向甲方的多次交付计划
- **交付说明** — 每次交付自动生成的甲方可读文档

**五种路径**（由自然语言自动路由，共用统一写入门禁）：
1. **初始化默认适配器** — 仅在无现有框架时创建 `branch_config.json` 和路线图
2. **交付建议** — 计算增量 → 给一条建议 → 完整 diff 预览 → 确认 → 写入并校验
3. **精确切换** — 验证指定状态 → 完整 diff 预览 → 确认 → 写入并校验
4. **状态查看** — 只读展示当前状态和待交付项
5. **交付说明生成** — 根据本次变更、上次快照或配置 diff 生成甲方说明

**核心设计原则**：现有框架优先、单一事实来源、一条推荐、统一预览确认、收益不得编造。

**对话示例**：
```
你：/0--laoyoutiao 快到交付节点了，这次交什么

Claude：[扫描 → 增量计算 → 叙事线分类...]

       老油条建议本次交付 — 性能优化线
       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       模块   当前 → 建议        收益摘要
       sort   basic → timsort    排序响应更稳定（待基准验证）
       cache  memory → redis     跨进程缓存可共享
       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       配置：现有 settings/feature_flags.py
       将同步生成交付说明并追加历史记录；确认后写入并运行原校验。

你：确认

Claude：已交付 — 性能优化线
       ✓ sort:  basic → timsort
       ✓ cache: memory → redis
       交付说明已生成 → delivery-notes/2026-06-29-性能优化.md
```

```
你：/0--laoyoutiao 帮我初始化

Claude：[扫描项目 Python 文件...]
       发现 4 个注册表：
         sort     → 3 个实现 (basic, timsort, parallel)     [性能]
         cache    → 3 个实现 (memory, redis, memcached)     [性能]
         renderer → 2 个实现 (dom, webgl)                   [体验]
         auth     → 2 个实现 (basic, oauth)                 [安全]

       已创建 branch_config.json，全部使用基础实现。
       交付路线图已生成，共 4 次交付计划。
```

```
你：/0--laoyoutiao 把排序切成 parallel

Claude：验证通过，sort 有 parallel 实现。
       分支切换完成
       ✓ sort: timsort → parallel
```

**输出产物**：
| 文件 | 说明 |
|------|------|
| 项目现有交付配置 | 优先复用的分支开关事实来源（存在时） |
| `branch_config.json` | 无现成框架时创建的回退分支开关配置 |
| `.laoyoutiao/delivery-log.json` | 交付历史追踪 |
| `DELIVERY_ROADMAP.md` | 交付路线图（初始化时生成） |
| `delivery-notes/YYYY-MM-DD-叙事线.md` | 每次交付的甲方说明 |

---

## leader — 一句话想法 → Agent 任务书

**场景**：你有一个想法但不想亲自盯着 agent 干活——把想法告诉 leader，它调研代码库、提问（≤5 个）、产出一份 ≤4000 字的任务书，另一个 agent 拿书独立跑完，中间不找你。跑完后 leader 回来验收，给你 5 行人话报告。

**定位**：独立方法论。三角色委托模型——领导（你）出想法，管理者（Claude/leader）调研写书验收，执行者（目标模式 agent）拿书独立执行。与 `/1-规划` 的区别：规划是同会话内交互式设计，leader 产出的是给**另一个 agent** 独立跑的自包含任务书。

**触发方式**：仅可由用户显式调用：`/leader` 后跟想法（一句话即可）。

**核心流程**（五步）：

1. **调研**：实测代码库——命令存在吗、基线数字多少、文档和实际差多少。能查的绝不问。行业知识联网查，查不到标"假设，未验证"
2. **提问**：一轮 ≤5 个，只问查不到且会改变任务书的。每个给 2-4 个选项 + 推荐。领导不在场就按默认走，写进"我替领导拍的板"
3. **写书**：≤4000 字，六节固定结构。执行型（能写出验收命令的）和探索型（调研/选型/找方案）自动分流
4. **交付**：只给三样——一句用法导读、任务书代码块（可整块复制粘贴）、一句收尾。一个目标一次粘贴
5. **验收**：管理者回来复跑明卷（书里的验收命令）+ 暗卷（2-3 条执行者看不见的抽查），给 ≤5 行人话报告

**任务书六节**：

| 节 | 内容 |
|---|---|
| 开头（无标题） | 执行者身份声明、为什么干、冲突让步顺序、法与情报分界 |
| 我替领导拍的板 | 每个未问出口的决定 + 默认值 + 猜错的代价；领导发前可改 |
| 界限 | 改动白名单（只用白名单）、冻结文件指纹、点名禁止的顺手活 |
| 现状与任务 0 | 实测数字 + 来源日期；任务 0 核对前提，对不上就停 |
| 任务 N | 目标带数字、从哪下手、死规矩、验收命令、反向验证 |
| 完成条件 | 两条硬指标（度量结果 + 守约束）、止损轮数、BLOCKED.md |

**两类型自动分流**：

| 类型 | 适用场景 | 验收特点 |
|---|---|---|
| 执行型 | 能写出验收命令的（加功能、修 bug、重构） | 硬指标 + 命令验收 + 反向验证 |
| 探索型 | 要答案本身的（调研、选型、该不该做 X） | 结论数上限 + 来源可复现 + 死路也算交付 |

**防作弊五道防线**：

1. **作弊达标**：基线不可退（测试数/覆盖率 ≥ 基线、skip=0），点名禁止具体姿势（`.skip`、mock 被测对象、`|| true`）
2. **幻觉命令**：每条命令亲手跑过，摸不到写进任务 0 让它查实
3. **失忆**：进度写 `PROGRESS.md`，断点续跑先读它别重做
4. **一条道走到黑**：同一验收连败 3 次换项；结果比基线差就回滚如实报告
5. **静默事故**：反向验证——亲手制造失败证明检查会响，贴输出

**对话示例**：

```
你：/leader 把用户模块的数据库查询改成异步，别搞挂现有 API

Claude：[调研] 扫描 user/ 目录 → 找到 12 处同步查询
        [实测] 跑测试拿基线：47 tests, 92% 覆盖率, skip=0
        [提问] ① 异步框架？推荐 asyncio + aiosqlite  ② 允许改接口签名吗？推荐不改  ③ 性能目标？
你：① 按推荐  ② 不改  ③ P99 < 50ms

Claude：[交付]
        用法：在目标模式 agent 输 `/goal`，粘贴下面整段。
        领导看三处：开头意图、"我替领导拍的板"、末尾完成条件。
        
        ```markdown
        你是执行者，这份文档是你唯一的任务来源...
        （≤4000 字完整任务书）
        ```
        
        跑完回来说一声，我来验收，给你 5 行内的人话报告。
```

```
你：/leader 这个项目该用 Redis 还是 Kafka 做消息队列？

Claude：[调研] 查当前项目依赖、查两个方案的适用场景
        [提问] ① 消息量级？ ② 延迟容忍度？ ③ 团队熟悉度？
你：① 日均 10 万条  ② 秒级可接受  ③ 都用过

Claude：[交付——探索型]
        完成条件换成：至少 3 条可复现结论，每条附来源日期
        防作弊主防编造：引用必须可查
        预算：最多烧 30 分钟，烧完交卷"目前最优 + 还没排除什么"
```

**输出产物**：一份 ≤4000 字的任务书（粘贴到目标模式 agent），执行后回来验收。

---

## multi-worker — 内置多 Agent 并行开发入口

**场景**：已有完整设计文档，且可以拆成 2–5 个互不修改同一文件的独立子任务，需要调用宿主内置 worker Agent 并行实现。

**位置**：多 Agent 协同入口。`multi-worker` 消费已确认任务，并管理 Agent 配置检查、worktree 隔离、并行派发、验收和集成。

**触发方式**：仅可由用户显式调用：`/multi-worker <已确认的 docs/plans/<feature>/tasks.md 路径>`。不因自然语言中的“并行开发”“分头开发”自动触发。

**输入门禁**：`docs/plans/<feature>/tasks.md` 必须已由用户确认，并逐项包含 Task ID、任务、允许修改的文件、验收标准和依赖关系。任务分支和 worktree 名不属于规划产物；用户确认派发后，`multi-worker` 根据 Task ID 与任务 slug 确定性生成并展示。缺失、未确认或任务不独立时停止，要求用户显式运行 `/1-规划` 生成并确认任务清单；`multi-worker` 不调用该 user-only 入口。

**前置门禁**：
1. 设计文档必须包含目标、模块划分、接口约定和验收标准。
2. 派发前必须枚举项目级与用户级 Agent 配置，核对 Agent 名称、工具权限、模型和 `worktree` 能力。
3. 未找到兼容的开发 worker 时停止并报告配置缺口；不得直接使用 Agent，也不得静默回退到通用 Agent。
4. 工作区必须干净；每个 worker 使用独立分支和 worktree。

**核心流程**：已确认的任务清单 → 独立性与 Agent 配置检查 → 用户确认派发 → Git 基线与任务分支 → 并行派发 → 逐项验收 → 合并 → 集成分支完整测试、适用 lint/type check 与最终 diff review → 清理。

任一 worker、合并或集成检查失败时保留 worktree 和集成分支，报告失败而不报告完成。

**对话示例**：
```
你：/multi-worker docs/plans/text-tools/tasks.md
Claude：已读取用户确认的 docs/plans/text-tools/tasks.md，包含 3 个互不冲突的任务。
        已确认 worker-dev 的工具权限、模型与 worktree 能力。
        请确认是否按该任务清单创建 worktree 并派发 3 个 worker？
```

**边界**：没有已确认的任务清单时停止并请用户显式运行 `/1-规划`；任务有强依赖则按依赖顺序执行。合并后必须先在集成分支完成完整测试、适用 lint/type check 和最终 diff review，全部通过后才能清理 worktree。

---

# 开发流程 Skills

> 按阶段 0~6 组织的项目开发全流程工具链。

---

## 0--claude — CLAUDE.md 初始化器

**场景**：开始新项目时初始化 AI 协作规则，或重置现有项目的 CLAUDE.md。一键生成，无需交互。

**触发方式**：`/0--claude`

**生成内容**（三个规则块 + 工作流路由）：
- **称呼规则** — 每次回复叫你 "luohe"，漏了说明上下文需要压缩/切换
- **Caveman 简洁规则** — 先说结论 / 简化思考 / 短句优先 / 不过度简化
- **Karpathy 编码准则** — 先想后写 / 简洁优先 / 外科手术式改动 / 目标驱动
- **工作流路由** — 按任务规模分档 + 支撑层触发表，让 AI 不必等用户报技能名
- **项目信息 + 命令速查** — 占位符，生成后可按需补充

**注入方式**：非破坏式。已有 CLAUDE.md 时只检测缺失的块并嵌入，已有内容完全不动。

**对话示例**：
```
你：帮我初始化这个项目的 CLAUDE 文件
Claude：luohe，检测到当前目录 "data-pipeline"。
       📄 CLAUDE.md 不存在 → 新建完整文件
       ✅ 已生成，包含称呼规则 + Caveman 简洁规则 + Karpathy 编码准则 + 工作流路由 → /1-规划
       可在"项目信息"和"命令速查"部分补充项目特定内容。
```

---

## 0--neat-freak — 知识库洁癖审查

**场景**：需要对整个项目知识体系（CLAUDE.md、docs/、agent记忆）做 OCD 级别的**洁癖审查与同步**——校准文档与代码一致性、尺寸体检、记忆毕业、消除矛盾。与 `6-最后整理` 的分工：`6` 沉淀本次会话产出（加法），neat-freak 校准全局知识库（减法/校准）。防止文档腐烂、记忆膨胀、跨会话信息丢失。

**阶段**：阶段 0 溢出技能（知识维护，部署名 `0--neat-freak`）

**触发方式**：`/0--neat-freak` 或用户说"同步一下、整理文档、梳理一下、收尾、这个阶段做完了"等

**核心机制**：三层知识分工、记忆毕业、先减后加。完整流程、尺寸阈值和自检项以 [`0--neat-freak/SKILL.md`](0--neat-freak/SKILL.md) 为准。

**对话示例**：
```
你：这个阶段做完了，帮我整理一下
Claude：luohe，已完成尺寸体检和知识文件盘点。
       - 删除过期状态与重复说明
       - 修正文档与同步映射冲突
       - 校验链接、路径和相对日期事实
```

---

## 0--tokenless — 超压缩沟通模式

**场景**：希望回复极度精简、减少 token，或要求 Caveman 风格表达时使用。

**阶段**：阶段 0 溢出技能（部署名 `0--tokenless`）

**触发方式**：`/0--tokenless`，或自然语言「caveman mode」「less tokens」「be brief」。

**核心规则**：
- 删除冠词、填充语、客套和模糊措辞，允许短句、片段、缩写和因果箭头
- 技术术语保持准确，代码块不变，错误原文精确引用
- 触发后持续生效，直到用户说 `stop caveman` 或 `normal mode`
- 安全警告、不可逆操作确认、易误读的多步骤流程暂时恢复完整表达，之后自动回到压缩模式

**对话示例**：
```
你：/0--tokenless Why React component re-render?
Claude：Inline obj prop -> new ref -> re-render. `useMemo`.
```

---

## 0-启动 — 项目脚手架

**场景**：开始一个全新的 Python 项目时使用。最小三件套：项目结构 → 本地 git → uv 环境。ruff / mypy / pre-commit 不默认装，要才加。

**触发方式**：`/0-启动`

**对话示例**：
```
你：/0-启动 新建一个项目叫 data-pipeline
Claude：新建 data-pipeline/ 子目录，还是就在当前目录初始化？
你：新建子目录
Claude：[uv init --package --vcs git → tests/ → uv add --dev pytest]
        1 passed，首次提交完成。要 ruff / mypy / pre-commit 说一声。
```

---

## 1-规划 — 方案设计与任务拆解

**场景**：写代码前方案不确定，需要系统性地理清需求、设计接口、输出 PRD、拆解任务。适合中型以上功能。

**触发方式**：`/1-规划`

**核心特性**（参考最新 grilling / grill-with-docs）：
- 🌳 **默认批量 grilling** — 按决策依赖树取当前 frontier，把主题相近、互不依赖的问题集中询问
- 🪜 **逐步模式** — 用户说“一步一步”“逐步完善”“一个一个问”等时，切换为一次一问
- 🔎 **事实/决策分流** — 代码和文档可查的事实自行调查；目标、偏好和取舍由用户决定
- 🔍 **文档发现** — 自动扫描 CONTEXT.md / ADR，交叉验证方案与已有领域模型
- 🎯 **场景压力测试** — 用具体场景探测概念边界和逻辑漏洞
- 🧪 **原型验证门禁** — 状态机、算法或 UI 假设不确定时，在 `docs/prototypes/<topic>/` 建立 throwaway prototype；结论回写 `docs/plans/<topic>/`，默认不进生产
- 📝 **单一领域文档** — 用户确认共享理解后，只增量维护 CONTEXT.md
- 📋 **确认门禁** — 用户确认共识后才生成规划产物；再次授权后才进入开发

**包含 6 个阶段**（按顺序推进）：
1. 方案 grilling — 先查事实，再批量询问同层决策；逐步意图触发一次一问；最后确认共享理解
2. 领域术语 — 只更新 CONTEXT.md，标记模糊词汇和歧义
3. 决策记录 — 对难逆转、少见、有真实取舍的决策创建轻量 ADR
4. 接口设计 — 并行生成多种方案对比
5. 输出 PRD — 产出结构化需求文档
6. 拆解任务 — 拆为可独立执行的垂直切片

**对话示例**：
```
你：/1-规划 帮我想想怎么做订单取消功能
Claude：[事实调查] 已从 CONTEXT.md 和代码确认订单状态与现有退款模块。

       [批量 grilling — 当前 frontier]
       1. 已支付订单取消后如何退款？
          推荐：自动原路退款 + 异步确认，避免人工状态不一致。
       2. 未支付订单取消是否允许恢复？
          推荐：不可恢复，重新下单，保持状态机简单。

你：1 按推荐，2 不允许恢复
Claude：[下一层 frontier] 如果订单已部分发货，取消范围如何处理？
       推荐：只取消未发货部分，已发货部分走退货流程。
...
Claude：[共享理解摘要] 目标、事实、关键决策、范围外事项、残余风险如下……
       是否确认这就是双方的共享理解？
你：确认
Claude：进入阶段 2，只增量更新 CONTEXT.md；随后生成 ADR、PRD 和任务清单。

[规划产物完成后再次询问是否授权进入开发]
```

若用户说”请一步一步逐步完善”，同一流程改为每轮只问一个决策并等待回答。

---

## 2-开发 — TDD 编码实现

**场景**：实现功能或修复 bug 时使用。pytest 驱动，红-绿-重构循环，贯穿四条编程准则。

**触发方式**：`/2-开发`

**准则**：先想后写 → 简单第一 → 手术刀式改动 → 目标驱动

**流程**：规划 → 示踪弹 → RED→GREEN 递增循环 → 重构 → 自检 → 携带审查基点和需求来源交给 `/3-检查` 正式验收。开发和审查默认保持改动未提交；审查通过后，仅在用户明确授权时调用 `/5-版本管理` 保存版本。

**对话示例**：
```
你：用 TDD 实现一个计算器 add 方法
Claude：接口确认：def add(a: int, b: int) -> int
       RED: test_add_returns_sum → 失败
       GREEN: def add(a, b): return a + b → 通过
```

---

## 3-检查 — 代码审查与验收

**场景**：合并代码前审查质量，发现问题时只建 issue，需要评估代码腐烂或模块臃肿，或需要找根因并立即修复。由输入契约自动路由：来自 `/2-开发` 或 `multi-worker` 且带审查基点、需求来源和 diff 审查意图时强制 Review；有现象且明确要记录/建 issue 时只建单；明确输入“架构评估”时只评估架构和测试 seam，产出 `docs/analysis/<topic>.md` 和 `docs/plans/<topic>/` 改造任务后停止；要求找根因或立即修复时转 `/4-调试`。只有输入确实模糊时才问一个澄清问题。

**触发方式**：`/3-检查`

**Review 输出**：必须输出 `PASS`、`PASS WITH WARNINGS` 或 `FAIL`。通过裁决和审查报告是可选进入 `/5-版本管理` 的正式交接产物，但进入版本管理仍须用户明确授权。

**对话示例**：
```
你：帮我检查一下 payment-service 分支
Claude：Standards 审查：
       ❌ processPayment → 应改为 process_payment
       Spec 审查：
       ❌ "退款原路返回" 未实现
```

---

## 4-调试 — 结构化调试

**场景**：遇到难复现的 bug 或性能突然下降时，用六阶段流程系统性地排查根因。

**触发方式**：`/4-调试`

**六阶段**：构建可比较的观测信号 → 复现 → 假设（3-5 条） → 工具验证 → 修复 + 回归测试 → 清理。调查可用 trace、metrics、安全采样、请求样本、profile、内存快照；稳定或统计可信复现用于修复验收，而非进入调查的前置。

**Python 工具**：breakpoint()、cProfile、py-spy、tracemalloc

**对话示例**：
```
你：订单支付接口偶尔返回 500，帮我调试
Claude：先构建回路——curl 循环调用 100 次...
       发现约 5% 失败。3 条假设：
       1. 连接池耗尽 2. 网关超时 3. 缓存穿透
```

---

## 5-版本管理 — Git 版本控制

**场景**：在用户明确授权后保存进度、查看历史、回滚版本、推送到 GitHub。`/2-开发` 和 `/3-检查` 不会默认提交；审查通过后由用户显式调用本技能才保存版本。

**触发方式**：`/5-版本管理`

**操作清单**：init / save / log / diff / rollback / reset / branch / remote / push / pull

**对话示例**：
```
你：帮我保存一下进度，我改了用户模块
Claude：git commit -m "feat: 完成用户模块基础功能"
你：看看改了什么
Claude：git log --oneline --graph
你：我想推送到 GitHub
Claude：仓库 URL 是什么？
```

---

## 6-最后整理 — 会话收尾与沉淀

**场景**：完成一个阶段、结束一天工作、或把项目交给别人时使用。管的是**本次会话的沉淀 + 物理整理**，动作偏加法。需要对整个知识库做洁癖同步（文档↔代码一致性、尺寸体检、记忆毕业）时，转 `0--neat-freak`。

**触发方式**：`/6-最后整理`

**四步骤**：
1. **修改总结** — 本次改了什么（文件/模块/关键决策/未完成项）
2. **经验沉淀** — 把跨会话可复用的教训、决策理由写进 agent memory；出现毕业信号则转 neat-freak
3. **项目结构整理** — 清临时文件、归位散落文件、检查目录结构（删除前先确认）
4. **交接 + 安全护栏** — 创建交接文档，确认 PreToolUse 钩子生效

**边界**：只处理本次会话产出 → 留在这里；要审查整个知识库是否腐烂 → 用 `0--neat-freak`。

**对话示例**：
```
你：今天做完了，帮我收尾
Claude：1. 修改总结 → 改了 payment 模块 3 个文件，决定用原路退款
        2. 经验沉淀 → memory 新增"退款网关有 5% 超时，需重试"
        3. 项目结构整理 → 删掉 scratch_test.py 等 2 个临时文件
        4. 交接 → docs/handoff/handoff-a7k3.md；安全护栏 → 已配置

        注：CLAUDE.md 里 payment 路由表看着有点旧，建议跑一次 0--neat-freak 做全局同步。
```

---

# My-Note 层（知识管理）

> 知识库管理技能体系，从 Obsidian vault 技能体系提炼。`noteall` 为入口路由器（用户调用），其余 14 个为内部技能（模型调用）。标记为 `layer: my-note`。

---

## noteall — 知识库路由器

**职责**：NL解析意图 → 提取参数 → 路由到对应流程。主流程：Intake → Compose → Polish → Index。

**调用方式**：用户显式调用 `/my-note/noteall`

**核心特性**：
- **环境检测** — 自动检测 vault 目录或读取默认知识库配置
- **NL路由** — 根据信号词（URL/日记/笔记/会议等）自动匹配技能
- **工作流路由** — 4阶段流程（Intake/Compose/Polish/Index）覆盖完整知识生命周期
- **快捷穿透** — `note 消化 {URL}`、`note 写关于{主题}` 直接路由

**示例**：
```
你：/my-note/noteall 消化 https://example.com/article
Claude：[检测环境→路由到 info-digester] 请确认：消化深度？（summary/detailed/atomic）
你：detailed
Claude：[创建来源笔记到 7-Sources/，含元数据/论点/wikilink]
```

```
你：/my-note/noteall 写一篇关于REST API设计的笔记
Claude：[路由到 note-composer] 为什么需要这篇笔记？笔记类型？
你：记录知识，知识概念
Claude：[盘问领域→撰写→推荐wikilink→生成到 4-Resources/]
```

---

## Intake 层（内容进入）

### info-digester — 外部信息消化
URL/PDF/文本→结构化笔记。3种深度：summary（300字摘要）/ detailed（来源笔记）/ atomic（来源+概念笔记）。

### raw-ingester — 原始数据摄入
raw/ 文件扫描→分步盘问→路由到 daily-concierge / info-digester / note-composer / workflow-wrapup。

### batch-curator — 批量知识整理
6阶段流水线：Scan→Analyze→Plan（一次确认）→Execute→Index→Archive。支持 .md/.csv/.json/.pdf/.pptx 等格式。

---

## Compose 层（笔记撰写）

### note-composer — 笔记撰写
盘问（类型/领域/深度）→匹配模板→生成笔记+wikilink→补全frontmatter。

### article-writer — 深度长文
5阶段：Research→Outline→Draft→Revise→Publish。面向2000+字深度内容。

### meeting-minutes — 会议纪要
自动链接参会人、提取行动项（含负责人+截止日期）、生成跟进清单。

### reading-digester — 阅读笔记
书籍/长文的高亮、批注、章节摘要、跨书概念引用、书架管理。

---

## Polish 层（质量提升）

### note-polisher — 笔记润色
6维检查（元数据/结构/链接/内容/格式/Confidence），自动修复+建议修复。批量模式评分 A/B/C/D。

### concept-atomizer — 概念原子化
长笔记→原子概念笔记，建立互链，生成/更新 MOC。

---

## Index 层（导航维护）

### index-keeper — 索引维护
全量/增量/健康检查/缺失补全 4种模式。只维护 _INDEX.md，MANUAL区域不碰。

### vault-cartographer — 知识库制图
MOC生成与更新、知识聚类发现、链接分析（中枢/入口/不对称）、导航健康诊断。

---

## 维护与收尾

### file-organizer — 文件整理
5维诊断→计划一次确认→批量执行。不删除，只归档。

### daily-concierge — 日记管家
每日日记+晚间反思+知识提炼+周/月回顾+待办追踪。

### workflow-wrapup — 收尾归档
确认产出→raw/归档到7-Sources/→生成报告→建议后续。
