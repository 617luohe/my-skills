# Skills 使用说明书

> 调用分类：仅用户调用的 skill 必须显式输入 canonical 名称；允许模型调用的 skill 可由模型按场景自动选择，也可显式调用。完整分类见 [README.md](README.md)。

---

## 🧭 不知道用哪个技能？

**先用 `/0-询问luohe`** — 技能路由器，包含主流程图 + 快速判断表。

---

# Vocabulary 层（可复用核心）

> 被其他技能调用的核心循环，不直接暴露给用户。标记为 `layer: vocabulary`。

---

## grilling — 询问循环

**职责**：relentless 询问循环，沿决策树逐个决议。

**被谁调用**：`/1-规划`

**核心特性**：
- **默认批量模式** — 集中询问 frontier 中独立决策（主题相近、互不依赖）
- **逐步模式** — 用户说"逐步"/"一步一步"时，一次一问
- **事实/决策分流** — 事实自行调查（代码/配置/文档），决策交给用户
- **推荐答案** — 每个决策先给推荐 + 理由

**示例**：
```
你：/1-规划 设计订单取消功能
Claude：[批量 grilling]
       1. 已支付订单取消后如何退款？推荐：自动原路退款。
       2. 未支付订单取消是否允许恢复？推荐：不可恢复。
你：1 按推荐，2 不允许恢复
Claude：[下一层 frontier] 如果订单已部分发货，取消范围如何处理？
```

---

## domain-modeling — 领域建模

**职责**：维护 `CONTEXT.md`（领域术语表）和 ADR（架构决策记录）。

**被谁调用**：`/1-规划`

**核心特性**：
- **术语冲突标记** — 用词与已有 CONTEXT.md 冲突时立即指出
- **模糊语言锐化** — 提议精确的规范术语（如"account"是 Customer 还是 User？）
- **场景压力测试** — 用具体场景探测边界
- **内联更新** — 术语确认后立即更新 CONTEXT.md
- **ADR 节制** — 只有（难逆转 + 少见 + 真实取舍）三条件全满足才创建 ADR

**CONTEXT.md 格式**：
```markdown
## Language
**Order**: A customer's request to purchase products.
_Avoid_: Purchase, transaction

## Relationships
- An **Order** produces one or more **Invoices**
```

---

## tdd — TDD 循环

**职责**：红-绿-重构循环，pytest 驱动。

**被谁调用**：`/2-开发`、`/multi-worker`

**核心特性**：
- **规划** — 列出要测试的行为（不是实现步骤）
- **示踪弹** — 写一个测试确认路径可行
- **递增循环** — RED→GREEN，一次一个测试，不超前实现
- **重构** — 全绿后提取重复、深化浅模块

**测试策略**：
- **简单任务**（<50行）→ 至少 1 个关键路径测试
- **中大任务**（新功能、重构）→ 3-5 个核心行为测试

**MUST 规则**：
- 绝不在 RED 时重构
- 测试通过公共接口验证行为，不验证实现细节

---

## code-review — 代码审查

**职责**：双轴审查（Standards + Spec），并行子代理。

**被谁调用**：`/2-开发`、`/3-检查`、`/multi-worker`

**核心特性**：
- **Standards 子代理** — 读规范（CLAUDE.md、CONTRIBUTING.md）+ diff，报告违反规范的地方
- **Spec 子代理** — 读需求（PRD、issue）+ diff，报告需求符合度
- **并行运行** — 两个子代理独立执行，避免互相污染
- **上下文管理** — diff <500行并行，≥500行串行

**审查维度**：
- 代码质量门禁（命名、类型、异常处理、import 组织）
- 功能目标门禁（性能指标、可靠性、资源消耗、覆盖率）

---

## diagnosing-bugs — Bug 诊断

**职责**：六阶段诊断流程，用于难复现 bug 和性能回归。

**被谁调用**：`/4-调试`

**六阶段**：
1. **构建反馈回路** — 建立可重复的失败信号（一条命令，运行必现）
2. **复现与最小化** — 缩小触发条件，找到最小复现路径
3. **假设（3-5 条）** — 列出可能的根因假设
4. **工具验证** — 逐个验证假设（`breakpoint()`、`cProfile`、`py-spy`、`tracemalloc`）
5. **修复 + 回归测试** — 修复根因并添加回归测试
6. **清理** — 删除调试代码

**MUST 规则**：
- 不构建反馈回路不假设
- 假设阶段至少列 3 条（避免锚定偏见）
- 一次只改一个变量验证
- 修复必带回归测试

---

# 独立方法论 Skills

> 不绑定开发阶段，跨场景通用的思维工具。

---

## 辩证矛盾分析法 — 矛盾分析思维框架

**场景**：遇到复杂问题需要系统性分析时使用。适合宏观判断、战略规划、根因分析、逆境突破、团队动员、决策复盘等场景。

**阶段**：独立方法论（无阶段编号）

**触发方式**：`/0--dialectic`

**核心流程**（六步法）：
1. **调查研究** — 先了解实际情况再发言
2. **定性** — 区分战略/战术、全局/局部
3. **矛盾分析** — 找主要矛盾，分内因外因，一分为二
4. **阶段划分** — 相持→反攻→收官，不同阶段不同策略
5. **策略制定** — 集中力量、统一战线、试点推广
6. **持续复盘** — 定期反思、知错即改

**语言风格**：比喻式（人民的语言）为主，搭配设问式、对比式、断言式、鼓舞式等多种句式。困境场景优先用经历共情式。

**违禁词**：严禁第一人称口吻、严禁政治立场表态、严禁敏感话题。

**对话示例**：
```
你：/0--dialectic 我们团队在 2026-07 的交付效率下降，开发节奏越来越慢
Claude：[调查研究] 我先看看你们团队的组织结构和技术栈……
        [定性] 看起来是组织协作问题，不是技术问题。
        [矛盾分析] 主要矛盾在于：需求和交付能力之间的对抗性矛盾。
        内因是团队沟通效率低，外因是需求方不断加急单。
        [策略] 建议分三步走：先统一需求入口（统一战线）……
```

```
你：/0-询问luohe（路由器）
Claude：[显示主流程图 + 快速判断表]
       你的情况：复杂战略问题 → 用 /0--dialectic
```

---

## 老油条 — 交付节奏管理

**场景**：面向甲方交付 Python 项目时，开发速度快于交付节奏，需要逐步展示优化成果、控制交付节奏、体现持续工作量。

**阶段**：独立方法论（无阶段编号）

**触发方式**：`/0--laoyoutiao <你的需求>`（允许模型按场景自动调用）

**核心机制**：
- **兼容优先** — 先识别并复用项目已有的特性开关、配置中心或实现选择框架；只有没有现成机制时才创建 `branch_config.json`
- **分支开关** — 由项目现有交付框架控制；回退模式使用 `branch_config.json` 中 `{"sort": "timsort"}` 式键值对
- **适配契约** — 统一整理当前状态、候选状态、变更方法、校验方法和收益来源
- **默认适配器** — 仅回退模式使用 Python 注册表和 `branch_config.json`
- **叙事线** — 升级项分类：性能、功能、体验、安全
- **交付路线图** — 面向甲方的多次交付计划
- **交付说明** — 每次交付自动生成的甲方可读文档

**五种路径**（由自然语言自动路由，共用统一写入门禁）：
1. **初始化默认适配器** — 仅在无现有框架时创建 `branch_config.json` 和路线图
2. **交付建议** — 计算增量 → 给一条建议 → 完整 diff 预览 → 确认 → 写入并校验
3. **精确切换** — 验证指定状态 → 完整 diff 预览 → 确认 → 写入并校验
4. **状态查看** — 只读展示当前状态和待交付项
5. **交付说明生成** — 根据本次变更、上次快照或配置 diff 生成甲方说明

**核心设计原则**：现有框架优先、单一事实来源、一条推荐、统一预览确认、收益不得编造。

**对话示例**：
```
你：/0--laoyoutiao 快到交付节点了，这次交什么

Claude：[扫描 → 增量计算 → 叙事线分类...]

       老油条建议本次交付 — 性能优化线
       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       模块   当前 → 建议        收益摘要
       sort   basic → timsort    排序响应更稳定（待基准验证）
       cache  memory → redis     跨进程缓存可共享
       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       配置：现有 settings/feature_flags.py
       将同步生成交付说明并追加历史记录；确认后写入并运行原校验。

你：确认

Claude：已交付 — 性能优化线
       ✓ sort:  basic → timsort
       ✓ cache: memory → redis
       交付说明已生成 → delivery-notes/2026-06-29-性能优化.md
```

```
你：/0--laoyoutiao 帮我初始化

Claude：[扫描项目 Python 文件...]
       发现 4 个注册表：
         sort     → 3 个实现 (basic, timsort, parallel)     [性能]
         cache    → 3 个实现 (memory, redis, memcached)     [性能]
         renderer → 2 个实现 (dom, webgl)                   [体验]
         auth     → 2 个实现 (basic, oauth)                 [安全]

       已创建 branch_config.json，全部使用基础实现。
       交付路线图已生成，共 4 次交付计划。
```

```
你：/0--laoyoutiao 把排序切成 parallel

Claude：验证通过，sort 有 parallel 实现。
       分支切换完成
       ✓ sort: timsort → parallel
```

**输出产物**：
| 文件 | 说明 |
|------|------|
| 项目现有交付配置 | 优先复用的分支开关事实来源（存在时） |
| `branch_config.json` | 无现成框架时创建的回退分支开关配置 |
| `.laoyoutiao/delivery-log.json` | 交付历史追踪 |
| `DELIVERY_ROADMAP.md` | 交付路线图（初始化时生成） |
| `delivery-notes/YYYY-MM-DD-叙事线.md` | 每次交付的甲方说明 |

---

## multi-worker — 内置多 Agent 并行开发入口

**场景**：已有完整设计文档，且可以拆成 2–5 个互不修改同一文件的独立子任务，需要调用宿主内置 worker Agent 并行实现。

**位置**：多 Agent 协同入口。`multi-worker` 管理内置 worker 的任务拆解、Agent 配置检查、worktree 隔离、并行派发、审查和合并。

**触发方式**：`/multi-worker <设计文档路径>`，或自然语言提到“并行开发”“分头开发”“同时开发多个独立模块”。

**前置门禁**：
1. 设计文档必须包含目标、模块划分、接口约定和验收标准。
2. 派发前必须枚举项目级与用户级 Agent 配置，核对 Agent 名称、工具权限、模型和 `worktree` 能力。
3. 未找到兼容的开发 worker 时停止并报告配置缺口；不得直接使用 Agent，也不得静默回退到通用 Agent。
4. 工作区必须干净；每个 worker 使用独立分支和 worktree。

**核心流程**：设计文档 → 独立任务拆解 → 用户确认 → Agent 配置检查 → Git 基线与任务分支 → 并行派发 → 逐项验收 → 合并。

**对话示例**：
```
你：/multi-worker docs/design-text-tools.md
Claude：设计文档可拆为 3 个互不冲突的任务。
        先检查 Agent 配置：已确认 worker-dev 的工具权限、模型与 worktree 能力。
        工作区和分支准备完成后，并行派发 3 个 worker；逐项测试通过后合并到集成分支。
```

**边界**：没有设计文档先用 `/1-规划`；任务有强依赖则先拆解并按依赖顺序执行。

---

# 工具类 Skills

> 11 个工具目录在 `skills-manifest.yaml` 中登记为 `host-provided`、`sync: false`，由宿主环境按需提供；精确镜像部署不会复制或改动其行为。

| 调用名 | 适用场景 |
|---|---|
| `/tools--前端设计` | 设计或重构生产级前端界面 |
| `/tools--图表生成` | 生成图表、信息图与可视化成品 |
| `/tools--幻灯片生成` | 创建或编辑演示文稿 |
| `/tools--技能工坊` | 创建、评估和改进 skill |
| `/tools--数据可视化` | 设计图表、仪表盘、配色与交互规范 |
| `/tools--文档生成` | 创建或编辑 Word 文档 |
| `/tools--智能搜索` | 按信息源路由并执行智能检索 |
| `/tools--深度研报生成` | 多来源研究并形成结构化研报 |
| `/tools--画布设计` | 创建画布式视觉内容 |
| `/tools--网页测试` | 驱动浏览器验证 Web 应用 |
| `/tools--表格生成` | 创建、编辑和分析 Excel 表格 |

---

# 开发流程 Skills

> 按阶段 0~9 组织的项目开发全流程工具链。

---

## use-skills — 智能技能调度器（入口）

**场景**：不知道用哪个技能？直接说需求，自动匹配。统一入口，无需记忆技能名称。

**触发方式**：`/use-skills <你的需求>`

**对话示例**：
```
你：/use-skills 分析这个项目中的代码结构
Claude：→ 分析需求：了解项目整体代码结构和模块划分
       → 匹配技能：2-分析（代码理解与概览）
       → 开始执行...
       [按 2-分析 流程输出模块地图 + 数据流]
```

```
你：/use-skills 帮我规划登录功能并用 TDD 实现
Claude：→ 匹配技能：1-规划 → [执行授权] → 4-开发
       [先规划方案并确认共享理解；展示 PRD/任务清单，获得执行授权后再 TDD 编码]
```

---

## 0--claude — CLAUDE.md 初始化器

**场景**：开始新项目时初始化 AI 协作规则，或重置现有项目的 CLAUDE.md。一键生成，无需交互。

**触发方式**：`/0--claude`

**生成内容**（三个规则块 + 工作流路由）：
- **称呼规则** — 每次回复叫你 "luohe"，漏了说明上下文需要压缩/切换
- **Caveman 简洁规则** — 先说结论 / 简化思考 / 短句优先 / 不过度简化
- **Karpathy 编码准则** — 先想后写 / 简洁优先 / 外科手术式改动 / 目标驱动
- **工作流路由** — 按任务规模分档 + 支撑层触发表，让 AI 不必等用户报技能名
- **项目信息 + 命令速查** — 占位符，生成后可按需补充

**注入方式**：非破坏式。已有 CLAUDE.md 时只检测缺失的块并嵌入，已有内容完全不动。

**对话示例**：
```
你：帮我初始化这个项目的 CLAUDE 文件
Claude：luohe，检测到当前目录 "data-pipeline"。
       📄 CLAUDE.md 不存在 → 新建完整文件
       ✅ 已生成，包含称呼规则 + Caveman 简洁规则 + Karpathy 编码准则 + 工作流路由 → /1-规划
       可在"项目信息"和"命令速查"部分补充项目特定内容。
```

---

## 0--neat-freak — 知识库洁癖审查

**场景**：需要对整个项目知识体系（CLAUDE.md、docs/、agent记忆）做 OCD 级别的**洁癖审查与同步**——校准文档与代码一致性、尺寸体检、记忆毕业、消除矛盾。与 `9-最后整理` 的分工：`9` 沉淀本次会话产出（加法），neat-freak 校准全局知识库（减法/校准）。防止文档腐烂、记忆膨胀、跨会话信息丢失。

**阶段**：阶段 0 溢出技能（知识维护，部署名 `0--neat-freak`）

**触发方式**：`/0--neat-freak` 或用户说"同步一下、整理文档、梳理一下、收尾、这个阶段做完了"等

**核心机制**：三层知识分工、记忆毕业、先减后加。完整流程、尺寸阈值和自检项以 [`0--neat-freak/SKILL.md`](0--neat-freak/SKILL.md) 为准。

**对话示例**：
```
你：这个阶段做完了，帮我整理一下
Claude：luohe，已完成尺寸体检和知识文件盘点。
       - 删除过期状态与重复说明
       - 修正文档与同步映射冲突
       - 校验链接、路径和相对日期事实
```

---

## 0--tokenless — 超压缩沟通模式

**场景**：希望回复极度精简、减少 token，或要求 Caveman 风格表达时使用。

**阶段**：阶段 0 溢出技能（部署名 `0--tokenless`）

**触发方式**：`/0--tokenless`，或自然语言「caveman mode」「less tokens」「be brief」。

**核心规则**：
- 删除冠词、填充语、客套和模糊措辞，允许短句、片段、缩写和因果箭头
- 技术术语保持准确，代码块不变，错误原文精确引用
- 触发后持续生效，直到用户说 `stop caveman` 或 `normal mode`
- 安全警告、不可逆操作确认、易误读的多步骤流程暂时恢复完整表达，之后自动回到压缩模式

**对话示例**：
```
你：/0--tokenless Why React component re-render?
Claude：Inline obj prop -> new ref -> re-render. `useMemo`.
```

---

## 0-启动 — 项目脚手架

**场景**：开始一个全新的 Python 项目时使用。最小三件套：项目结构 → 本地 git → uv 环境。ruff / mypy / pre-commit 不默认装，要才加。

**触发方式**：`/0-启动`

**对话示例**：
```
你：/0-启动 新建一个项目叫 data-pipeline
Claude：新建 data-pipeline/ 子目录，还是就在当前目录初始化？
你：新建子目录
Claude：[uv init --package --vcs git → tests/ → uv add --dev pytest]
        1 passed，首次提交完成。要 ruff / mypy / pre-commit 说一声。
```

---

## 1-规划 — 方案设计与任务拆解

**场景**：写代码前方案不确定，需要系统性地理清需求、设计接口、输出 PRD、拆解任务。适合中型以上功能。

**触发方式**：`/1-规划`

**核心特性**（参考最新 grilling / grill-with-docs）：
- 🌳 **默认批量 grilling** — 按决策依赖树取当前 frontier，把主题相近、互不依赖的问题集中询问
- 🪜 **逐步模式** — 用户说“一步一步”“逐步完善”“一个一个问”等时，切换为一次一问
- 🔎 **事实/决策分流** — 代码和文档可查的事实自行调查；目标、偏好和取舍由用户决定
- 🔍 **文档发现** — 自动扫描 CONTEXT.md / ADR，交叉验证方案与已有领域模型
- 🎯 **场景压力测试** — 用具体场景探测概念边界和逻辑漏洞
- 📝 **单一领域文档** — 用户确认共享理解后，只增量维护 CONTEXT.md
- 📋 **确认门禁** — 用户确认共识后才生成规划产物；再次授权后才进入原型或开发

**包含 6 个阶段**（按顺序推进）：
1. 方案 grilling — 先查事实，再批量询问同层决策；逐步意图触发一次一问；最后确认共享理解
2. 领域术语 — 只更新 CONTEXT.md，标记模糊词汇和歧义
3. 决策记录 — 对难逆转、少见、有真实取舍的决策创建轻量 ADR
4. 接口设计 — 并行生成多种方案对比
5. 输出 PRD — 产出结构化需求文档
6. 拆解任务 — 拆为可独立执行的垂直切片

**对话示例**：
```
你：/1-规划 帮我想想怎么做订单取消功能
Claude：[事实调查] 已从 CONTEXT.md 和代码确认订单状态与现有退款模块。

       [批量 grilling — 当前 frontier]
       1. 已支付订单取消后如何退款？
          推荐：自动原路退款 + 异步确认，避免人工状态不一致。
       2. 未支付订单取消是否允许恢复？
          推荐：不可恢复，重新下单，保持状态机简单。

你：1 按推荐，2 不允许恢复
Claude：[下一层 frontier] 如果订单已部分发货，取消范围如何处理？
       推荐：只取消未发货部分，已发货部分走退货流程。
...
Claude：[共享理解摘要] 目标、事实、关键决策、范围外事项、残余风险如下……
       是否确认这就是双方的共享理解？
你：确认
Claude：进入阶段 2，只增量更新 CONTEXT.md；随后生成 ADR、PRD 和任务清单。

[规划产物完成后再次询问是否授权进入原型或开发]
```

若用户说“请一步一步逐步完善”，同一流程改为每轮只问一个决策并等待回答。

---



**场景**：设计方案中有不确定的决策（数据模型、状态机、算法、API 设计），先用一次性代码快速验证，再进入正式开发。

**触发方式**：`/3-原型`

**两种模式**：
- **逻辑原型** — CLI 脚本验证数据模型/状态机/算法
- **UI 原型** — Web 页面验证交互（按需使用）

**对话示例**：
```
你：/3-原型 帮我验证一下这个订单状态机设计
Claude：你的设计中，订单状态是 pending → paid → refunding → refunded，
       退款完成后还能再次退款吗？
       我先写个原型跑一下看看...
       [创建 prototype_order_state.py]
       State: pending → pay() → State: paid
       State: paid → refund() → State: refunding
       State: refunding → complete_refund() → State: refunded
       State: refunded → refund() → Error: 已退款订单不可再退款
       验证通过。结论已记录，原型已清理。
       是否授权按该设计进入 4-开发？
```

---

## 2-开发 — TDD 编码实现

**场景**：实现功能或修复 bug 时使用。pytest 驱动，红-绿-重构循环，贯穿四条编程准则。

**触发方式**：`/2-开发`

**准则**：先想后写 → 简单第一 → 手术刀式改动 → 目标驱动

**流程**：规划 → 示踪弹 → RED→GREEN 递增循环 → 重构

**对话示例**：
```
你：用 TDD 实现一个计算器 add 方法
Claude：接口确认：def add(a: int, b: int) -> int
       RED: test_add_returns_sum → 失败
       GREEN: def add(a, b): return a + b → 通过
```

---

## 3-检查 — 代码审查与验收

**场景**：合并代码前审查质量，或者发现 bug 时报 issue。

**触发方式**：`/3-检查`

**两个模式**：
- **代码审查** — 双轴并行（Python 规范 + 需求符合度）
- **Bug 报告** — 你口述问题，Claude 探索代码并提交 GitHub issue

**对话示例**：
```
你：帮我检查一下 payment-service 分支
Claude：Standards 审查：
       ❌ processPayment → 应改为 process_payment
       Spec 审查：
       ❌ "退款原路返回" 未实现
```

---

## 4-调试 — 结构化调试

**场景**：遇到难复现的 bug 或性能突然下降时，用六阶段流程系统性地排查根因。

**触发方式**：`/4-调试`

**六阶段**：构建反馈回路 → 复现 → 假设（3-5 条） → 工具验证 → 修复 + 回归测试 → 清理

**Python 工具**：breakpoint()、cProfile、py-spy、tracemalloc

**对话示例**：
```
你：订单支付接口偶尔返回 500，帮我调试
Claude：先构建回路——curl 循环调用 100 次...
       发现约 5% 失败。3 条假设：
       1. 连接池耗尽 2. 网关超时 3. 缓存穿透
```

---

## 5-版本管理 — Git 版本控制

**场景**：保存进度、查看历史、回滚版本、推送到 GitHub。默认本地仓库，按需连接远程。

**触发方式**：`/5-版本管理`

**操作清单**：init / save / log / diff / rollback / reset / branch / remote / push / pull

**对话示例**：
```
你：帮我保存一下进度，我改了用户模块
Claude：git commit -m "feat: 完成用户模块基础功能"
你：看看改了什么
Claude：git log --oneline --graph
你：我想推送到 GitHub
Claude：仓库 URL 是什么？
```

---

## 6-最后整理 — 会话收尾与沉淀

**场景**：完成一个阶段、结束一天工作、或把项目交给别人时使用。管的是**本次会话的沉淀 + 物理整理**，动作偏加法。需要对整个知识库做洁癖同步（文档↔代码一致性、尺寸体检、记忆毕业）时，转 `0--neat-freak`。

**触发方式**：`/6-最后整理`

**四步骤**：
1. **修改总结** — 本次改了什么（文件/模块/关键决策/未完成项）
2. **经验沉淀** — 把跨会话可复用的教训、决策理由写进 agent memory；出现毕业信号则转 neat-freak
3. **项目结构整理** — 清临时文件、归位散落文件、检查目录结构（删除前先确认）
4. **交接 + 安全护栏** — 创建交接文档，确认 PreToolUse 钩子生效

**边界**：只处理本次会话产出 → 留在这里；要审查整个知识库是否腐烂 → 用 `0--neat-freak`。

**对话示例**：
```
你：今天做完了，帮我收尾
Claude：1. 修改总结 → 改了 payment 模块 3 个文件，决定用原路退款
        2. 经验沉淀 → memory 新增"退款网关有 5% 超时，需重试"
        3. 项目结构整理 → 删掉 scratch_test.py 等 2 个临时文件
        4. 交接 → handoff-a7k3.md；安全护栏 → 已配置

        注：CLAUDE.md 里 payment 路由表看着有点旧，建议跑一次 0--neat-freak 做全局同步。
```
