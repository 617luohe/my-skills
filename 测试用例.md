# Skills 体系优化 - 测试用例

## 测试目标

验证优化后的 use-skills 能正确判断任务规模，并自动路由到合适的 skill 或 skill 链。

---

## 测试用例集

### 分类 A：简单任务（单一 skill 路由）

| 测试ID | 用户输入 | 预期规模判断 | 预期路由 | 验证点 |
|--------|---------|-------------|---------|--------|
| A1 | "修改 config.py 的超时时间" | 简单 | 2-开发 | 单文件小改动 |
| A2 | "修复 auth.py:45 的空指针" | 简单 | 4-调试 | 已定位 bug |
| A3 | "给 User 类加个 email 字段" | 简单 | 2-开发 | 明确小改动 |
| A4 | "这段代码看不懂" | 简单 | [已废弃-代码分析功能] | 单一分析任务 |
| A5 | "把日志级别改成 DEBUG" | 简单 | 2-开发 | 配置调整 |
| A6 | "快速验证这个状态机设计" | 简单 | [已废弃-原型验证功能] | 单一验证 |

---

### 分类 B：中等任务（工作流串联）

| 测试ID | 用户输入 | 预期规模判断 | 预期路由 | 验证点 |
|--------|---------|-------------|---------|--------|
| B1 | "给系统加个缓存功能" | 中等 | 1-规划 → [执行授权] → 2-开发 → 3-检查 → 5-版本管理 | 新功能开发；规划后等待授权 |
| B2 | "实现用户认证" | 中等 | 1-规划 → [执行授权] → 2-开发 → 3-检查 → 5-版本管理 | 新功能，需规划与授权 |
| B3 | "优化性能" | 中等 | [已废弃-优化功能] → 2-开发 → 3-检查 | 方案不确定，先分析 |
| B4 | "重构支付模块，太乱了" | 中等 | [已废弃-优化功能] → 2-开发 → 3-检查 | 重构需先分析 |
| B5 | "订单和支付打通" | 中等 | 1-规划 → [执行授权] → 2-开发 → 3-检查 → 5-版本管理 | 多模块联动；规划后等待授权 |
| B6 | "调研竞品，做成PPT" | 中等 | tools--深度研报生成 → tools--幻灯片生成 | 跨工具串联 |
| B7 | "搜索最新技术方案，画架构图" | 中等 | tools--智能搜索 → tools--图表生成 | 信息+文档串联 |

---

### 分类 C：复杂任务（先规划并按授权分阶段执行）

| 测试ID | 用户输入 | 预期规模判断 | 预期路由 | 验证点 |
|--------|---------|-------------|---------|--------|
| C1 | "自动帮我提升测试覆盖率到 90%" | 复杂 | 1-规划 → [执行授权] → 2-开发 → 3-检查 | 先拆解范围和验收门禁，不自动闭环 |
| C2 | "端到端完成缓存层开发和测试" | 复杂 | 1-规划 → [执行授权] → 2-开发 → 3-检查 | 多阶段任务先规划并等待授权 |
| C3 | "修复所有 mypy 类型报错" | 复杂 | 1-规划 → [执行授权] → 4-调试 → 2-开发 → 3-检查 | 先盘点和拆批，再逐阶段执行 |
| C4 | "给 5 个模块都加上日志" | 复杂 | 1-规划 → [执行授权] → 顺序执行，或确认独立后用 multi-worker | 依赖任务顺序执行；独立任务才并行 |
| C5 | "持续改进代码质量" | 复杂 | 1-规划 → [执行授权] → 明确的阶段链 | 持续意图不等于无限自动执行 |

---

### 分类 D：边界场景

| 测试ID | 用户输入 | 预期规模判断 | 预期路由 | 验证点 |
|--------|---------|-------------|---------|--------|
| D1 | "看看这个项目" | 简单 | [已废弃-代码分析功能] | 模糊需求，默认分析 |
| D2 | "帮我改改代码" | 询问澄清 | 追问后路由 | 需求过于模糊 |
| D3 | "接手这个陌生项目" | 中等 | [已废弃-代码分析功能] | 直接读取仓库并建立项目地图 |
| D4 | "搜一下、分析、再开发" | 中等 | 明确串联 | 用户显式指定多步骤 |

---

### 分类 E：多 Agent 协作入口

| 测试ID | 用户输入 | 预期规模判断 | 预期路由 | 验证点 |
|--------|---------|-------------|---------|--------|
| E1 | "按设计文档分头并行开发三个独立模块" | 多 Agent 协作 | multi-worker | 先检查 Agent 配置，再派发内置 worker |
| E2 | "让多个 worker 同时实现这些互不冲突的任务" | 多 Agent 协作 | multi-worker | Agent 名称、权限、模型、worktree 均需核验 |
| E3 | "并行开发"但没有设计文档 | 多 Agent 协作 | multi-worker | 停止并要求先提供设计文档，不直接派发 |
| E4 | "并行开发"但无兼容 Agent 配置 | 多 Agent 协作 | multi-worker | 报告配置缺口，不静默回退通用 Agent |
| E8 | "自动帮我把覆盖率提升到 90%" | 复杂 | 1-规划 → [执行授权] → 2-开发 → 3-检查 | 无多 Agent 意图，复杂任务先规划 |
| E9 | "分析这个项目" | 简单 | [已废弃-代码分析功能] | 不因 agent 泛词误触发 |
| E10 | "让两个 agent 同时修改 auth.py" | 多 Agent 协作 | 拒绝并行写 | multi-worker 子任务独立性门禁生效 |

---

## 验证清单

优化后，AI 应该：

### ✅ 判断能力
- [ ] 能识别简单任务（单文件、已定位、明确小改动）
- [ ] 能识别中等任务（新功能、方案不定、多模块）
- [ ] 能识别复杂任务（多阶段目标、门禁反馈、多个任务）
- [ ] 能识别边界场景（模糊需求、陌生项目）
- [ ] 能识别已配置的 Claude 内置 worker 并行开发意图

### ✅ 路由能力
- [ ] 简单任务 → 直接路由到单个 skill
- [ ] 中等任务 → 规划 → 用户确认共享理解与执行授权 → 开发 → 检查 → 版本管理
- [ ] 复杂任务 → 1-规划 → 用户确认任务拆解与执行授权 → 按依赖顺序分阶段执行
- [ ] Claude 内置 worker 并行开发 → multi-worker，先检查 Agent 配置
- [ ] 模糊需求 → 追问澄清后再路由

### ✅ 执行能力
- [ ] 每个 skill 执行完毕后简短汇总
- [ ] 串联时按顺序执行，不并行
- [ ] 最终给出整体总结
- [ ] 复杂任务规划时说明理由，并在执行前等待授权

### ✅ 用户体验
- [ ] 输出格式统一：`→ 任务规模 → 分析需求 → 匹配技能 → 开始执行`
- [ ] 判断理由清晰（"含'自动帮我'关键词 + 需要多轮验证"）
- [ ] 无需用户指定 skill 名称
- [ ] 路由表引用一致（CLAUDE.md 引用 use-skills）

---

## 回归测试

修改后需确保原有功能不受影响：

| 原有功能 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 显式调用 skill | `/1-规划` | 直接进入该 skill |
| use-skills 单一匹配 | `/use-skills 分析代码` | [已废弃-代码分析功能] |
| use-skills 多技能串联 | `/use-skills 规划并实现` | 1-规划 → 等待执行授权 → 2-开发 |
| 各 skill 的"完成后推进" | 2-开发完成 | 自动进 3-检查 |
| 分阶段工作流按授权推进 | `/use-skills 自动完成缓存层开发和测试` | 1-规划 → 等待执行授权 → 2-开发 → 3-检查 |

---

## 性能指标

| 指标 | 目标 | 测量方法 |
|-----|------|---------|
| 路由准确率 | ≥ 95% | 测试用例通过率 |
| 用户满意度 | 无需手动指定 skill | 用户反馈 |
| 路由时间 | < 3秒 | 从输入到开始执行 |
| 表达一致性 | 100% | CLAUDE.md 和 use-skills 路由表同步 |

---

## 已知限制

1. **任务规模判断依赖关键词**：如果用户描述不含触发词，可能误判
2. **中等和复杂任务需要显式授权**：规划完成后不会自动进入执行阶段
3. **复杂任务不自动无限闭环**：检查或门禁失败后报告结果，由用户授权返工或下一阶段

---

## 下一步优化方向

1. **引入上下文感知**：根据当前项目状态（有无 CLAUDE.md、代码规模）调整判断
2. **学习用户偏好**：记录用户常用路径，优化路由建议
3. **动态串联**：根据中间结果调整后续 skill（如规划时发现需要原型验证）
