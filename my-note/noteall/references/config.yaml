# Noteall Vault 解析配置（唯一事实源）
# 仓库内不保存任何个人 Vault 路径。解析顺序：
#   1. prefer_current_vault 为 true 且当前目录含 .obsidian/ → 用当前目录
#   2. 环境变量 NOTEALL_VAULT
#   3. 宿主本地配置 references/config.local.yaml 的 vault_path（不随仓库分发）
# 三者都不可用 → 停止并给出配置指引，不猜测其它目录。
prefer_current_vault: true
vault_path: ""
